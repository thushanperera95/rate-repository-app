import { StyleSheet, View } from "react-native";

const styles = StyleSheet.create({
  separator: {
    width: 10,
  },
});

const HorizontalItemSeparator = () => <View style={styles.separator} />;

export default HorizontalItemSeparator